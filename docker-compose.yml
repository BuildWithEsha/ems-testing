version: '3.9'

services:
  ems-app-testing:
    build:
      context: .
      dockerfile: Dockerfile
    image: ems-app-testing:latest
    container_name: ems-app-testing
    ports:
      - "${PORT:-5001}:5000"
    environment:
      NODE_ENV: production
      PORT: ${PORT:-5000}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_PORT: ${MYSQL_PORT:-3306}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_CONNECT_TIMEOUT: ${MYSQL_CONNECT_TIMEOUT:-60000}
      MYSQL_CHARSET: ${MYSQL_CHARSET:-utf8mb4}
      MYSQL_CONNECTION_LIMIT: ${MYSQL_CONNECTION_LIMIT:-10}
      TEAMLOGGER_API_KEY: ${TEAMLOGGER_API_KEY}
    volumes:
      - uploads_data_testing:/app/uploads
    restart: unless-stopped
    networks:
      - mysql_network

volumes:
  uploads_data_testing:

networks:
  mysql_network:
    external: true